
language: python
python:
  - "3.6"

before_script:
 - cd $PROJECT_DIR/model

script: pytest

before_deploy:
 - cd $PROJECT_DIR/model

deploy:
  provider: pypi
  user: $PYPI_USER
  password: $PYPI_PASS
  on:
    tags: true